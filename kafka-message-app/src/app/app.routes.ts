import { Routes } from '@angular/router';
import { MessageProducerComponent } from './message-producer/message-producer.component';

export const routes: Routes = [
  { path: '', component: MessageProducerComponent }
];
